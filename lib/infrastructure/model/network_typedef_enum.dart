enum Endpoint { login, create, users, logout, delete }

enum ResourceType { login, admin, users, logout }

enum ResourceContentType { json, form, webclient }

enum RequestType {
POST,
GET,
PATCH,
DELETE,
PUT,
}
