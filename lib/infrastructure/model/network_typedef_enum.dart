enum Endpoint { login, create, users, logout, delete }

enum ProviderType { keyclock, commonservice, rtc, gms, cms }

enum ResourceType { login, admin, users, logout }

enum ContentType { json, form }

enum ApiMethod {
  POST,
  GET,
  PATCH,
  DELETE,
  PUT,
}

enum RequestType {
POST,
GET,
PATCH,
DELETE,
PUT,
}
